<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorBackgroundFloating">@color/colorBackgroundFloating</item>
        <item name="android:textSize">@dimen/default_text_size</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="RadioButtonStyle">
        <item name="android:layout_marginStart">@dimen/spacing_size_3</item>
        <item name="android:layout_marginEnd">@dimen/spacing_size_3</item>
        <item name="android:layout_marginBottom">@dimen/spacing_size_4</item>
        <item name="android:paddingStart">@dimen/spacing_size_1</item>
        <item name="android:buttonTint">@color/colorAccent</item>
    </style>

    <style name="DetailTextView">
        <item name="android:layout_margin">@dimen/spacing_size_3</item>
    </style>

    <style name="DetailLabel" parent="DetailTextView">
        <item name="android:textColor">@color/labelColor</item>
    </style>

    <style name="DetailValue" parent="DetailTextView">
        <item name="android:textColor">@color/colorPrimaryDark</item>
    </style>
</resources>
